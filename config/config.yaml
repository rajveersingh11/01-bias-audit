data:
  raw_path: "data/adult_income.csv"
  processed_path: "data/processed/clean.csv"
  source: "scikit-learn/adult-census-income"
  split: "train"
  source_url: null

protected_attributes:
  - sex
  - race
  - marital.status

model:
  name: "distilbert-base-uncased-finetuned-sst-2-english"
  batch_size: 128    # increased from 32 for 4x throughput
  max_length: 64     # reduced from 128 to halve attention computation

report:
  output_path: "outputs/"
  title: "Bias Audit Report"
  author: "Your Name"

thresholds:
  demographic_parity: 0.1
  disparate_impact: 0.8
  equal_opportunity: 0.1